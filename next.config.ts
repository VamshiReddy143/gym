import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["media1.giphy.com","lh3.googleusercontent.com","imgs.search.brave.com",'res.cloudinary.com'],
  }
};

export default nextConfig;
